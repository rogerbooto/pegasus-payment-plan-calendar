{
  "adapter": "stripe-hosted",
  "describes": "the total and a BNPL provider marker are present, but no bound instalment phrase is on THIS page -- the schedule finalizes inside the provider redirect. The engine still honestly surfaces what it has (a labelled total) rather than degrading entirely.",
  "expectedKind": "PARTIAL",
  "expectedMissing": ["installmentCount", "cadence", "perInstallment"],
  "expected": {
    "orderTotalCents": 15000,
    "currency": "USD"
  }
}
